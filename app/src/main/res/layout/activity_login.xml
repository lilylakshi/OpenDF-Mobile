<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".activities.LoginActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="vertical">

        <score.gsoc.opendf.widgets.CustomLightTextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:gravity="center"
            android:textSize="32sp"
            android:textColor="@color/dark_blue_color"
            android:fontFamily="sans-serif-light" />

        <score.gsoc.opendf.widgets.CustomLightEditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/username"
            android:textColor="@color/dark_blue_color"
            android:inputType="text"
            android:fontFamily="sans-serif-light"
            android:textCursorDrawable="@drawable/dark_blue_cursor" />

        <score.gsoc.opendf.widgets.CustomLightEditText
            android:id="@+id/login_password_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/password"
            style="@style/EditTextdark_blue"
            android:inputType="textPassword"
            android:fontFamily="sans-serif-light"
            android:textCursorDrawable="@drawable/dark_blue_cursor" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/loginButton"
            android:text="@string/login"
            android:textColor="@android:color/white"
            android:textAllCaps="true"
            android:fontFamily="sans-serif-light"
            android:onClick="authenticateUser"
            android:textSize="@dimen/button_text_size" />
    </LinearLayout>
</RelativeLayout>
